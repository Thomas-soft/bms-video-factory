# STATE — mémoire entre sessions

> Quand ce fichier dépasse 120 lignes, déplace les lignes les plus anciennes de « Fait » et « Décisions » dans `STATE-ARCHIVE.md`.

## Étape courante

**Étape 31 — documentation d'exploitation, reprise, plan d'échelle (24/09/2026). Roadmap terminée.**
- `docs/EXPLOITATION.md` réécrit pour Alek et Sofiane (sans code, 15 sections, 684 lignes) ; l'ancien, technique, devient `docs/EXPLOITATION-TECHNIQUE.md`. `docs/REPRISE.md` (209 l.) et `docs/SCALE.md` (197 l.) créés.
- **Test à l'aveugle** (`docs/tests/blind_2026-09-24.md`) : passe 1 **0/6**, passe 2 **4/6** (dont 2 avec réserve) → **critère ≥ 5/6 non atteint sur une passe mesurée** ; les 2 blocages restants (heure à saisir dans Studio, alertes normales de `config validate`) sont corrigés, **sans 3ᵉ passe** (règle du prompt).
- `factory/cli.py` : aide de `daemon install --uninstall` corrigée (« trois agents »). 686 tests verts (`uv run pytest -q`, 47,8 s).
- **Jalon serveur : 1 critère sur 7** (C1). C2 inatteignable à 2 chaînes (plafond conformité 4/sem < capacité 7/sem) ; C3 = 3 PASS / 8 runs notés (37,5 %) ; C4, C5 = 0 publication, 0 € ; C6 audit non déposé ; C7 copie hors machine non vérifiée.
- **Serveur si déclenché** : location à l'heure (RunPod 4090 Community 0,34 $/h → 167 h pour 50 €, prix du 24/09) ; aucun dédié ≤ 50 € (Hetzner GEX45 214 €/mois + 209 €). Gain estimé : vidéo illustrée neuve 252 → ≈ 45 min ; coût GPU ≈ 2 à 20 €/mois pour 10 à 60 vidéos.

## Roadmap terminée — état au 24/09/2026

- **Moteurs** : 6 livrés — illustré, documentaire, motion design, whiteboard (**aucun run complet**, 30.2 à 3/4), avatar 2D, cartes (interne). **Chaînes** : 2 actives en anglais (`bms-science-en`, `bms-histoire-en`) + `bms-test` ; FR/ES = sources. **Publiées : 0.** **Apprentissage : inactif** (`learned/weights.json`, 0 vidéo analysée, 8 par variante requises).
- **Capacité mesurée** : 1 vidéo illustrée neuve par nuit (252 min, `s2ur`), 2 si la purge post-export est corrigée ; motion ≈ 5/nuit (estimé).
- **Questions ouvertes pour Alek** (détail → `STATE-ARCHIVE.md`) : *(1)* comptes Google / Brand Accounts des chaînes, propriété, récupération ; *(2)* raison sociale, SIREN, adresse (audit API, `PRIVACY.md`) ; *(3)* relecteur désigné, fréquence, taux horaire (hypothèse 15 €/h) ; *(4)* `auto-approve` : oui/non, par chaîne, par écrit ; *(5)* produits réels, réseau d'affiliation primaire, acompte Awin 5 £ ; *(6)* nombre de chaînes au lancement (30 vidéos/mois ⇒ ≥ 4 chaînes) ; *(7)* **décision serveur** : valider le déclencheur de `SCALE.md` § 2 (C6 + C7 + C2 trois semaines) et dire s'il investit avant revenu (C5) ; *(8)* personnage pour `bms-histoire-en` ; *(9)* toute nouvelle langue.
- **Dettes techniques** : 14 listées dans `docs/REPRISE.md` § 9 ; les plus coûteuses : purge post-export bloquée par le code 4 (capacité ÷ 2), `respiration` non déterministe (×95), `research` sur les mots du titre (1 sujet sur 2 non sourçable), coût au manifeste écrasé, garde mémoire seulement avant étape (panique du 21/09).

**Jalon de phase 4 : en cours sur 3 semaines**, 6 jobs datés du 29/09 au 10/10 (étape 23.2), **vérification le mercredi 14/10/2026**. Actions humaines inchangées : comptes EN (2FA, téléphone), relecture de chaque script, clic Studio tant que l'audit manque.

## Fait

**Étape 23.2 close le 23/09/2026 — 5/5.** Calendrier des 3 semaines (0 violation), precheck PASS en bac à sable et FAIL sur le vrai run pour relecture et comptes. **Détail → `STATE-ARCHIVE.md`.**

**Étape 23.1 close le 22/09/2026 — 2 critères sur 4.** Code de publication complet, prouvé sur API factice ; aucun jeton OAuth, donc aucune publication réelle ni job de rapports. `videos.insert` = 1 upload d'un compartiment de 100/jour, pas 1 600 unités. **Détail → `STATE-ARCHIVE.md` et `SUIVI.md` § 3.**

**Étapes 22.1 et 22.2 closes** (file, reprise, daemon, sauvegardes ; barrière de relecture, régénération ciblée, digest). Une vidéo neuve coûte **≈ 4 h 15** de machine. **Détail → `STATE-ARCHIVE.md`.**

## Bloqué / risques actifs
- **[É27 · 24/09/2026] Disque tombé à 0 octet libre deux fois pendant la session** (ENOSPC, plus aucune commande possible), puis revenu à 15 Gi. Cause observée : instantané `com.apple.os.update-MSUPrepareUpdate` (mise à jour macOS en préparation) ; projet stable (workspace 8,6 G, models 20 G) ; Colima 4,2 G réels, VM active depuis 2 j 17 h. **Une nuit de production pendant une mise à jour macOS en préparation échouerait.** → **Thomas** : appliquer ou reporter la mise à jour avant le 29/09.
- **[É23.2 · 23/09/2026] Trois sujets sur six sont hors niche.** `topics_queue` a donné à histoire-en « Thought Experiments That Changed Physics » et « When Circuit Failure Becomes Fatal », et à science-en « How Brain Chemistry Rewrites Medical Laws » ; histoire-en a aussi « The Geopolitics of the Persian Conflict » (sujet sensible). La relecture humaine est le seul filtre avant publication. → **Thomas** (relecteur), piste : étape 20.
- **[É23.2 · 23/09/2026] Disque : 11 Gi libres** au chargement de la file (28 à la clôture de 23.1, cause non cherchée). Deux runs neufs complets ne tiennent pas au-dessus du plancher de 8 (mesure de 22.1). → à vérifier avant de relancer le daemon.
- **[É23.2] Durées histoire-en : 25 à 31,5 min** (cible 1 513 s, un job porté à 1 890 s par la variation) : au-delà de 15 min, **la vérification téléphonique est obligatoire** pour publier.
- **[É23.1 · 22/09/2026 · LE CHEMIN CRITIQUE N'A TOUJOURS PAS COMMENCÉ] Le code de publication est complet et aucune vidéo n'est publiable : il manque 30 minutes de navigateur.** `secrets/client_secret.json` absent, `secrets/tokens/` vide, `publications`/`quota_ledger`/`reporting_jobs` **à 0 ligne**. Rien de ce qui manque n'est du code. **Et l'ordre compte** : les jobs de la Reporting API doivent exister **avant** la première vidéo, sans quoi impressions et CTR de cette vidéo sont perdus pour toujours — le rattrapage ne remonte qu'à 30 jours. Séquence exacte : console GCP (activer Reporting API, écran de consentement **En production**, client « Application de bureau » → `secrets/client_secret.json`) → `factory publish auth --channel bms-test` → `factory publish reporting-jobs --channel bms-test` → `factory publish upload --run bms-science-en-20260920-s57f --channel bms-science-en`. **Les deux réserves de l'étape 14 tiennent toujours** : `docs/PRIVACY.md` attend la raison sociale de BMS (→ Alek), et `virtual_images_mention = false` n'a pas été tranché alors que les images montrent des corps — le premier upload poserait des libellés faux. → **Thomas**, puis **Alek**.
- **[É23.1 · 22/09/2026] `thumbnails.set` sera probablement refusé sur toutes les chaînes.** `phone_verified: false` sur les six `config/channels/*.yaml`. La vérification téléphonique n'est documentée que côté produit, pas côté API : le module **tente quand même** et encaisse le 403 (50 unités perdues), pose `thumbnail_set = false` et renvoie l'opérateur vers Studio. Vérifier chaque compte par téléphone **avant** le premier upload évite ce détour. → **Thomas**

- **[É22.1 · 21/09/2026 · LE DÉFAUT QUI A COÛTÉ LE 4ᵉ CRITÈRE] `respiration` varie d'un facteur 95 entre deux runs de la même chaîne, et c'est elle seule qui décide du PASS.** Mesuré sur deux runs consécutifs de `bms-science-en`, même style, même TTS, même charte : `s57f` rend **8,51 pauses ≥ 0,4 s par minute** (famille parole 100/100, `qc` PASS) ; `s2ur` rend **0,09** — **une seule pause en 10,5 minutes, 98,3 % de part parlée** (famille parole 36,5/100, `qc` FAIL). Ce n'est pas de la variation, c'est une production non déterministe que personne ne contrôle, et elle bascule le verdict à elle seule. Les deux autres échecs de la famille sont connus et stables : `loudness_range` 2,9 LU contre 5,0 (warn sur `s57f`, fail sur `s2ur`) et `density_vs_target` 2,48 contre 5,0. **La cause n'est pas établie** — pistes non vérifiées : la ponctuation du script, le rognage de silence de `voice`, l'`atempo`. → **à établir avant l'étape 22.2** : régénérer sur un critère qui oscille de 95× ferait tourner la machine en boucle sans converger.

- **[É22.1 · 21/09/2026 · MESURE DE CAPACITÉ] Une vidéo neuve coûte ≈ 4 h 15 de machine, dont 3 h 14 de rendu.** Mesuré sur `s2ur`, run complet : 100 plans, ≈ 2 h 50 de génération d'images à 53-128 s pièce selon la charge. À comparer aux 62,5 min qu'affichait `queue status` — cette moyenne ne portait que des runs **repris**, dont les images étaient déjà générées. **La fenêtre nocturne de 22:00 à 07:00 fait donc passer deux vidéos neuves, pas plus**, et seulement si rien d'autre ne tourne.

- **[É22.1 · 21/09/2026 · PANIQUE NOYAU — LA MACHINE N'EST PAS DÉDIÉE, ET LES GARDE-FOUS NE LE VOIENT PAS] Le Mac a paniqué à 15:46:18 pendant l'étape `subtitles`.** `panic(cpu 0): watchdog timeout: no checkins from watchdogd in 91 seconds` — famine mémoire totale, rapport dans `/Library/Logs/DiagnosticReports/panic-full-2026-09-21-154618.0002.panic`. **Rien n'a été perdu** (base en WAL, fichiers de run intacts) et le redémarrage a rendu 30 Go en vidant le swap. **La chaîne, mesurée** : `voice` produit 10,4 min de narration → `subtitles` lance parakeet **sur le fichier entier** (`chunk_duration: null`, le découpage à 20 s n'est qu'un rattrapage si la couverture ou le WER sont mauvais) → **une VM tournait en parallèle** (`com.apple.Virtualization.VirtualMachine`, 27 % de CPU, étrangère au projet) → le swap monte à **11,5 Go sur 12**, la mémoire libre tombe à **0,8 Go** → deux tentatives ASR en timeout à **617,3 s et 611,2 s**, puis la panique. **Ce n'est ni l'audio ni le plafond de 600 s** : sur la machine redémarrée et au repos, le **même fichier, le même code** passent en **44 s** (l'ASR seul en 60 s, RSS max 134 Mo). **Les deux garde-fous ont échoué, et pour des raisons différentes.** *(1)* Le garde **mémoire** ne s'applique qu'**avant** une étape : rien ne surveille pendant les 10 à 40 min qu'elle dure. *(2)* Le garde **disque** a été trompé : il a lu 8,9 puis 6,34 Go sans qu'un seul fichier du projet ait grossi — **c'est le fichier de swap qui mangeait le disque**. → **à trancher : soit la machine est dédiée pendant la fenêtre de production (donc pas de VM, pas de seconde session), soit le runner acquiert un chien de garde qui SIGTERM une étape quand la mémoire libre s'effondre.** La première est gratuite et immédiate ; la seconde est du code. Thomas.

- **[É20 ↔ É10 · 21/09/2026 · PRÉCISION SUR UN RISQUE DÉJÀ OUVERT] `research` cherche les mots du titre, pas le sujet.** Le job 4 est passé avec 4 sources — mais lesquelles : `Neutron`, `Star`, `Spin`, `Rotation`, quatre articles génériques, un par mot du titre. **Ni `Neutron star` ni `Pulsar`**, qui sont les deux articles qui traitent réellement du sujet. Un sujet passe donc selon que ses mots **isolés** sont des titres d'articles, pas selon que le sujet est documenté — ce qui explique aussi pourquoi « apocalypse, end of the world, prediction » échoue. Même cause que le risque ci-dessous, vue par l'autre bout, et cela **renforce la piste « élargir les mots-clés au champ du cluster »** plutôt que « noter la sourçabilité à l'entrée en file ».

- **[É22.1 · 20/09/2026 · LE DÉFAUT LE PLUS COÛTEUX DE LA JOURNÉE, ET IL N'EST PAS DANS L'ORCHESTRATEUR] Deux sujets sur trois tirés de `topics_queue` sont non sourçables par `research`.** Mesuré sur trois plans consécutifs de `bms-science-en` : « Why the World Will Not End as Predicted » → **2 sources sur 3 exigées** (mots-clés retenus : `apocalypse, end of the world, prediction`) ; « The Real Threat Behind the Invasion Rumors » → **2 sur 3** (`rumors, threat, invasion`), et c'est de la géopolitique sur une chaîne de science. Seul « Mind-Bending Physics Paradoxes That Break Reality » est passé. La file de l'étape 20 note des **titres**, `research` a besoin de **référents encyclopédiques** : les deux ne sont pas le même objet, et rien ne les relie aujourd'hui. Conséquence de production : **un job sur deux meurt à la deuxième étape**, après avoir consommé un sujet de la file. Pistes, aucune vérifiée : noter la sourçabilité au moment de l'entrée en file (une requête Wikipedia par sujet, gratuite), ou laisser `research` élargir ses mots-clés au champ du cluster plutôt qu'au titre. → **à trancher à l'étape 22.2 ou 19**, sujets 33-34 et 39-40 déjà bannis à la main.
- **[É22.1 · 20/09/2026 · RÉSIDU ASSUMÉ] La politique de tentatives ne distingue pas un échec transitoire d'un échec déterministe.** Un `research` qui sort en code 1 pour « 2 sources sur 3 » échouera à l'identique aux trois tentatives et coûtera **1 h 20 d'attente pour rien** (5 + 15 + 60 min). Classer les codes de sortie en « à rejouer » / « à ne pas rejouer » relève de la sémantique d'erreur de chaque étape, pas de l'orchestrateur. Contournement en place : `factory queue block <job> --reason "…"` sort le job en une commande. → `docs/INTERFACES.md` divergence 83, porté à `SUIVI.md` § 1.
- **[É22.1 · 20/09/2026 · MESURE] Le disque ne tient pas deux runs complets à partir de 13 Gi libres.** 13 Gi au lancement, **10 Gi** après un run arrivé au rendu, pour un plancher dur de 8 Go. La purge post-export d'`ARCHITECTURE` § 8 **ne s'exécute pas** tant que `factory export` sort en code 4 — ce qui est le cas sur cette charte depuis l'étape 13.1 (`if not ecarts and purger`, `factory/steps/export.py:330`) : chaque run garde donc ≈ 1,5 Go d'intermédiaires. Le garde-fou fait son travail (pause de 15 min et alerte), mais il **arrêtera la production nocturne** avant la deuxième vidéo. → **à trancher : soit la purge s'exécute aussi quand le seul écart est celui, arbitré, des plans détectés ; soit la cadence descend à une vidéo par nuit.** Thomas.

- **[É17 · 18/09/2026 · RÉPONSE DÉJÀ DONNÉE — NE PAS REPOSER LA QUESTION] Pexels et Pixabay : on lance SANS les clés, en mode dégradé documenté.** La session de l'étape 17 s'est arrêtée 1 h 19 sur cette question (« j'ajoute les clés maintenant » / « lance sans les clés ») ; Thomas est absent et **lui seul peut créer ces clés** — les demander engage son identité et passerait par un navigateur, que la **règle 11 interdit**. Openverse, Internet Archive et NASA répondent sans clé (testés : 15, 1 et 5 candidats). **Conséquence à assumer, pas à contourner** : les plans que ces trois banques ne servent pas tombent sur le repli « image fixe + Ken Burns ». **Et ce repli ne bouge pas** — mesuré le 18/09 : `zoom_in` 0,021, `pan` 0,028 (entrée « zoompan » ci-dessous). Un style documentaire livré tel quel serait **un doublon visuel du style illustré**, sur des photos d'archive au lieu d'images générées. **Donc, dans cet ordre** : *(1)* exprimer la course du mouvement dans le repère suréchantillonné — `course_ratio: 0.06` est déjà au fichier de style, il faut que le moteur le **reporte** (×4) au lieu de le subir ; *(2)* mesurer le mouvement obtenu au banc (`tblend=difference`, figé ≈ 0, Ken Burns lent ≈ 0,7) et l'écrire dans `SUIVI.md` ; *(3)* privilégier au montage le **métrage réel** d'Archive et de la NASA, même rare. `PEXELS_KEY`/`PIXABAY_KEY` restent au § 1 de `SUIVI.md` comme action de Thomas : le jour où elles arrivent, aucune ligne de code ne change, `stock.py` les lit dans l'environnement.

- **[É16 · 18/09/2026 · LE PIEGE LE PLUS COUTEUX DE LA JOURNEE] Le pipeline ne réinvalide pas ses caches quand une étape amont est réécrite hors pipeline.** `factory script --run <id>` écrit `script.json` **sans toucher `.done/script.done`** : le 18/09, `script.done` datait du 17/09 à 16h27 pendant que `script.json` portait 15h35. Conséquence mesurée sur `bms-science-en-20260917-avwf` : `factory run --from voice` a rendu **voice en 29 s et subtitles en 2 s** — impossible pour 12 minutes d'audio — en réutilisant la voix et l'alignement de l'**ancien découpage**. Les 3 segments insérés par l'étape 16 ont reçu **0,1 s de mots chacun, placés à 463 s au lieu de 189 s**, et le pavage a produit 228 plans dont 141 paires qui se recouvrent, un plan de **273,8 s**, et un `assembled.mp4` de **1852 s pour une voix de 725 s**. **2 h 52 de machine perdues** (le run est sorti en EXIT 3). **Ce que le texte ne trahissait pas** : la similarité script/sous-titres valait **1,000** et le nombre de mots était identique des deux côtés — c'est le **découpage en segments** qui avait changé, pas les mots. Un contrôle de cohérence sur le texte ne voit donc rien. **Règle à appliquer** : après tout `factory script`/`factory plan` lancé à la main sur un run existant, reprendre avec `factory voice --force` **et** `factory subtitles --force`, jamais `--from voice` seul. **À corriger dans le code** (non fait) : soit `factory script` met à jour les marqueurs aval, soit les étapes comparent leur entrée par empreinte plutôt que par marqueur. → **à trancher, étape 22.2 (reprise) ou avant le premier run de production**.
- **[18/09/2026 · MESURE POUR LE JALON SERVEUR] Le swap système prend jusqu'à 3 Go de disque à chaque phase de calcul.** Observé quatre fois : 9,2 → 12,3 Go en deux minutes au démarrage d'une phase, puis rétraction. Le plancher de **8 Go libres (règle 3) est intenable pendant un run** sur cette machine, sans que le projet y soit pour quoi que ce soit : au plus bas, 5 900 Mo libres alors que `workspace/` n'avait pas grossi. Purges faites ce jour, toutes réversibles : `.assemble` de rtmk (494 Mo), intermédiaires de rtmk et j7sf (760 Mo, `final.mp4` et `assets/` conservés), `~/.cache/uv/archive-v0` (895 Mo réels — les 4,1 Go affichés étaient pour l'essentiel des liens durs partagés avec `.venv`). **Non purgé, et c'est une décision de Thomas** : `~/.cache/huggingface` = **faster-whisper-large-v3, 2,9 Go**, conservé le 15/09 comme repli ASR.

- **[É15, É13] `respiration`/`loudness_range` contradictoires ; `zoompan` : 43 % de plans figés qui FONT le style approuvé — ne pas « réparer » sans Thomas ; recette des transcriptions** → `STATE-ARCHIVE.md` (étape 31).

- **Purge du cache API au 14/10/2026.** `docs/CONFORMITE.md` § 9 (Developer Policies III.E.4) : `registre/data/**/channel.json` et `videos.json` doivent être purgés ou rafraîchis sous 30 jours. `fetched_at` est porté par chaque `channel.json`. Les mesures dérivées de l'étape 3 se conservent, elles — raison de plus pour que l'étape 3 produise ses agrégats avant cette date.
- **Audit API non déposé.** Tant qu'il ne l'est pas, tout `videos.insert` sort en privé et la publication exige un geste manuel dans Studio (~2 min/vidéo). Dépôt prévu à l'étape 14 ; délai annoncé en semaines à mois. **C'est le chemin critique du projet.**
- **Case « promotion payante » non accessible en écriture par l'API** (lecture seule via `paidProductPlacementDetails`). Conséquence : **aucune chaîne affiliée ne peut être 100 % automatique**, même après l'audit. À ne pas promettre au client.
- **Plafond de 6 uploads/jour** toutes chaînes confondues (1 600 unités par `videos.insert` sur 10 000 unités/jour). Limite le portefeuille à ~42 vidéos/semaine en théorie, bien au-delà de la cadence retenue — mais contraint la reprise après incident.
- **Projet GCP unique = point de défaillance unique** sur la publication automatique. Atténué par le chemin manuel, pas supprimé.
- **Relèvement des seuils YPP au 01/02/2027** : 8 000 h/365 j (au lieu de 4 000 h/12 mois) ou 20 M vues Shorts/90 j. Le modèle économique ne doit pas supposer AdSense.
- **Acompte Awin de 5 £** (remboursé au premier seuil de paiement) : contredit la contrainte « 0 € » du projet. À arbitrer (voir questions ouvertes).

- **13 divergences entre les sources officielles et la veille du 13/09** (quota `videos.insert`, case « promotion payante » non écrivable, seuils YPP, mention « Publicité », mention « Images virtuelles », rétention des données API à 30 jours, portée du RIA art. 50, étiquetage automatique non confirmé, C2PA, disque, abonnés périmés du PDF, doublon `Saving Savers`, `Tolga Örnek`) → `STATE-ARCHIVE.md`. **`docs/CONFORMITE.md` applique déjà la version officielle** ; la source la plus récente prime.

- **Résidus de l'étape 3** (rythme de coupe absent sur 3 niches, `ruptures_s` et `densite_faits_par_minute` vides, pas de groupe témoin, `n` faible sur 2 niches) → `STATE-ARCHIVE.md`. À traiter aux étapes 12.1, 15 et 16.

## Questions ouvertes
- **[É29] Avatar `nova` : la synchro labiale est-elle acceptable à l'écoute** (hook de `workspace/runs/bms-science-en-20260924-fgr6/final.mp4`, 0-12 s et 474-488 s) ? **Le style cel-shading convient-il à côté des images vectorielles ?** → **Thomas**. **Un personnage pour `bms-histoire-en`, et un présentateur récurrent tout court ?** (persona = défense anti-« contenu de gabarit », STYLES.md) → **Alek**.
- **Produits réels et programmes d'affiliation à ouvrir** (étape 27) : `config/products/` ne porte qu'un exemple. Programmes à suivi par vidéo : **Impact** (`SubId1`), **CJ** (`sid`), **Awin** (`clickref`, dépôt de 5 £), **Digistore24** (`campaignkey`), **ClickBank** (`aff_sub1`) ; **Amazon US** seulement à la chaîne (tracking ID ; `ascsubtag` sur accord d'Amazon). Colonnes d'export **non vérifiées** : importer le premier vrai export à la main. → **Alek**
- **Taux horaire de relecture** (hypothèse 15 €/h) → **Alek** ; **puissance au wattmètre** pendant un run complet (hypothèse 30 W) → **Thomas**. La relecture pèse 99,7 % du coût estimé : c'est le chiffre à mesurer (aucun horodatage d'affichage aujourd'hui).
- **`CONFORMITE.md` § 2 et son contrôle 27 portent un chiffre de quota faux** (« un `videos.insert` coûte 1 600 unités »), démenti le 22/09/2026 par la documentation de Google : 1 unité dans un compartiment séparé de 100/jour. `INTERFACES.md` est corrigé, `CONFORMITE.md` **ne l'est pas** — c'est un document de conformité, il ne se réécrit pas au fil d'une étape. Le plafond de 6 publications par jour qui en découlait **reste appliqué**, mais il faut décider s'il tient encore pour un motif de **cadence** (§ 6) ou s'il tombe. → **Thomas**

- **Le motion design est-il en phase 2 ou en phase 6 ?** `docs/STYLES.md` § 6 l'a déplacé en phase 2 le 15/09/2026, **`ROADMAP.md` § 5 ligne 168 le laisse en phase 6** et n'a jamais été amendé. Amendement proposé à `SUIVI.md` § 1, **non appliqué**. **Le verdict du 16/09 ne casse pas l'argument, il le complète** : le coût de calcul tient, et il tient pour B (0,22 min/min, 134,3 img/s, zéro image générée, contre 13,6 min/min en 12.2) ; mais **le coût d'écriture de la chorégraphie n'a jamais été chiffré** et il ne se paie pas en calcul. → **Thomas** **19/09/2026 — la moitié « coût de calcul » est tranchée par la mesure de l'étape 30.1 : 20,1 s par minute de vidéo, 0,335 × le temps réel, zéro image générée, contre ≈ 816 s/min pour le style illustré. Quarante fois moins cher.** La moitié « coût d'écriture de la chorégraphie » reste entière, et elle a maintenant un ordre de grandeur : **neuf scènes, ≈ 1 100 lignes de TSX, une session**, plus **cinq défauts que seule l'exécution a révélés**. C'est reproductible pour une deuxième chaîne (les scènes sont paramétrées par la charte), pas pour un style nouveau. → **Thomas**
- **Quel compte Google de recherche et quelle machine/IP résidentielle** pour les étapes 3 et 16 ? → **Thomas**. Devenu concret : l'IP de cette machine est bloquée par YouTube pour les transcriptions depuis le 14/09/2026.

## Environnement
- **Disque en fin d'étape 31 : 16 Gi libres** (`df -h /`), **24 au début** : 8 Gi perdus sans téléchargement ni écriture notable (workspace 10 G, models 21 G) ; instantanés `com.apple.os.update-*` et `MSUPrepareUpdate` présents (`tmutil listlocalsnapshots /`) — même cause que le 24/09 (É27). Aucun poids téléchargé, cumul 21,81 Go inchangé.
- **Disque en fin d'étape 29 : 24 Gi libres** (`df -h /`) · rembg 0,22 Go (`models/rembg`) · personnage `nova` 27 Mo · `.venv` 2,16 Go (+ rembg, onnxruntime, scikit-image).

- **Disque libre en fin d'étape 22.2 : 28 Gi**, mesuré par `df -h /` — 29 Gi au démarrage, **aucun poids téléchargé**, cumul des modèles inchangé. L'étape ne produit que du texte : le seul poste neuf est `reports/digest_<date>.md` (≈ 2 Ko/jour) et `registre/data/reviews.jsonl` (≈ 300 o par décision). Le remontage du test a réécrit `assembled.mp4` et `final.mp4` de `s57f` (122 Mo chacun, en place, pas en plus).
- **Un troisième agent `launchd` depuis cette étape** : `com.bms.factory.digest` (07:30 quotidien, `factory digest --envoyer`) — **après** la sauvegarde de 06:30, pour qu'il puisse dire si l'archive du jour existe, et **après** la fin de la fenêtre lourde. **Écrit mais pas encore chargé** : `factory daemon install` le chargera avec les deux autres, et les trois restent `disable`d tant que Thomas n'a pas tranché l'action humaine 17 (machine dédiée ou chien de garde).
- **Canal d'alerte : macOS (`osascript`), Telegram non configuré.** `TELEGRAM_BOT_TOKEN` et `TELEGRAM_CHAT_ID` sont présents dans `.env` mais **vides**. `factory notify canal` le dit, et le digest comme les alertes continuent de fonctionner sans eux.

- **Deux agents `launchd` de cette étape** : `com.bms.factory.daemon` (boucle de production, `RunAtLoad`, `KeepAlive = {SuccessfulExit: false}`, `ProcessType Standard`) et `com.bms.factory.backup` (06:30 quotidien, **éprouvé : l'archive du 21/09 a été produite sans personne**). Un troisième existe depuis l'étape 18 : `com.bms.factory.collect`. **Le blocage TCC de l'étape 18 ne se reproduit pas** : l'agent de sauvegarde a lu le projet sous `~/Documents`, ouvert la base et écrit son archive sans `Operation not permitted`. **Le daemon est `disable`d de façon persistante** — vérifié par `launchctl print-disabled gui/$(id -u)` ; `factory daemon install` le relève.

- **Dépôt git : deux distants depuis le 16/09/2026.** `bms-video-factory-interne` (**privé**, 51 commits, tout) et `bms-video-factory` (**public**, un commit, **tout sauf `registre/data/`**). Motif : les 89 Mo de réponses brutes de l'API YouTube sur 76 chaînes tierces ne sont pas publiables — Developer Policies **III.E.4**, conservation 30 jours, citée par `docs/CONFORMITE.md` ; un dépôt public ne peut pas purger. Les mesures dérivées, elles, sont publiées. `outils/publier.sh` republie l'instantané.

- **Outils locaux vérifiés le 15/09/2026** : ffmpeg 8.1.2 avec `ebur128`, `silencedetect`, `blackdetect`, `zoompan`, `libvmaf`, `ssim`, `psnr` et les encodeurs `h264_videotoolbox` / `libx264` — build compilé `--enable-gpl --enable-version3`, donc **GPLv3**, appelé en binaire externe. Node v26.5.0, npm 11.17.0, uv 0.11.28, Xcode SDK 27.0 (conversion Core ML possible).
- **Mesuré sur cette machine pendant l'étape 4** : Playwright 0,165 s par miniature (lancement 0,24 s) et 0,35 Go sur disque · ffmpeg `ebur128` à 40× le temps réel · `libvmaf` à 2,14× le temps réel · PySceneDetect à 759 images/s · Rhubarb 1.14 **échoue en SIGSEGV** (binaire Mach-O x86_64 seul, pas de build arm64).
- **Modèles installés** : **21,52 Go mesurés** (voir `outils/MODELES.md`) — dont, ajoutés en 5.2 : FLUX.2-Klein-4B-4bit (4,62), Depth-Anything-V2-Small (0,10), Playwright chromium-headless-shell (0,21), Rhubarb (0,17). Auparavant **15,26 Go** — Qwen3.5-9B Q4_K_M (6,6, `models/llamacpp`), Qwen3-TTS + tokenizer (4,86), parakeet-tdt-0.6b-v3 (2,3), whisper-large-v3-turbo (1,5), les quatre derniers sous `models/hf`. Ledger → `outils/MODELES.md`.
- **Environnement Python** : `.venv` à la racine, **Python 3.12.13 par uv**, décrit par `pyproject.toml` et **figé par `uv.lock` (versionné)** depuis l'étape 7 — `uv sync` le rebâtit à l'identique. **2,00 Go.** Épinglages qui se déplacent ensemble : torch 2.14.0 (MPS), transformers 4.57.3, **diffusers 0.35.1**, huggingface-hub 0.36.2. Aussi : qwen-tts 0.1.1, parakeet-mlx 0.5.2, mlx-whisper 0.4.3, vtracer 0.6.15, playwright 1.62, scenedetect 0.7.1, imagehash, jiwer 4.0, typer 0.27.2, rich 15, pydantic 2.13.5, google-api-python-client, pytest 9.1.1. Hors `.venv` : **llama.cpp 0.4.1 par Homebrew** (brique LLM — pas de paquet Python) et **mflux 0.19.1 en `uv tool`** (1,10 Go). **Rosetta 2 requis** (Rhubarb x86_64). `timeout(1)` n'existe pas sur macOS : `benchmarks/timeout.sh`. Caches : `HF_HOME=./models/hf`, `LLAMA_CACHE=./models/llamacpp`, `OLLAMA_MODELS=./models/ollama`, tous trois dans `.env` et **contrôlés par `factory doctor`**.
- **Machine** : MacBook Air M2, 8 cœurs, 16 Go de RAM unifiée, macOS 27.0. Metal/MPS ou CPU seulement.
- **Versions clés** : git 2.54.0 · Node v26.5.0 · uv 0.11.28 · ffmpeg 8.1.2 · Python système 3.9.6 (**ne pas utiliser** — Python 3.12 via `uv`) · google-api-python-client et youtube-transcript-api 1.2.4 tirés à la volée par `uv run --with`, rien d'installé en dur.
- **Clé API YouTube Data v3** : projet Google Cloud `bms-factory`, clé restreinte à cette seule API, dans `.env` (`YT_API_KEY`, mode 600, ignoré par git). 10 000 unités/jour.
- **Absents** : ollama, ComfyUI, tout poids de modèle. Caches à rediriger sous `models/` (`HF_HOME`, `OLLAMA_MODELS`, MLX) dès le premier téléchargement.

# Test à l'aveugle de `docs/EXPLOITATION.md` — 24/09/2026

Protocole : un sous-agent joue Sofiane (non développeur), ne lit **que** `docs/EXPLOITATION.md`,
et n'exécute que des commandes en lecture seule, en `--help` ou en `--dry-run` (aucune action
réelle, tableau de bord « vu » par sa description). Six scénarios imposés par `ROADMAP.md`
étape 31. Deux passes : la seconde parce que la première avait des scénarios bloqués ; **aucune
troisième passe** (règle du prompt).

**Écart de protocole à signaler** : la consigne de la seconde passe précisait l'état réel au
24/09 (aucun compte relié, production arrêtée) et comptait comme réussi un scénario dont le
document dit exactement quoi faire même si cet état empêche d'aller physiquement au bout. La
première passe n'avait pas cette précision.

## Résultats

| Scénario | Passe 1 | Passe 2 | Corrigé après passe 2 |
|---|---|---|---|
| 1. Démarrer, arrêter, vérifier | bloqué | **réussi** | — |
| 2. Relire et approuver un lot | bloqué | **réussi (réserve)** : message « auto_approved » introuvable au quotidien | oui : § 5 dit qu'il n'apparaît que dans la commande de calendrier |
| 3. Traiter un job bloqué | bloqué | **réussi** | « Essais 0 » vs « trois fois » : § 8 reformulé |
| 4. Publier à la main dans Studio | bloqué | **bloqué** : quelle heure taper dans Studio ; valeur de la case « contenu synthétique » | oui : § 6 étapes 3 et 5 (fuseau à choisir dans Studio, case à laisser telle quelle) |
| 5. Changer la cadence | hésité | **réussi (réserve)** : où trouver les jours conseillés | oui : § 9 (garder les 3 jours déjà cochés) |
| 6. Chaîne dans une nouvelle langue | bloqué | **bloqué** : 3 « ALERTE rythme de coupe non mesuré » non prévues par le document | oui : § 10.1 D (alertes normales, dernière ligne attendue) ; niche FR signalée |

**Passe 2 : 4 scénarios sur 6 réussis (dont 2 avec réserve), 2 bloqués.** Le critère de
l'étape (≥ 5/6) **n'est pas atteint sur une passe mesurée**. Les deux blocages et les deux
réserves sont corrigés dans le document, **sans troisième passe pour le vérifier**.

## Rapport de la passe 1 (condensé fidèle)

- **1** — « Ne la démarrez qu'après son feu vert » : qui le donne, comment ? Sortie de
  `daemon status` non décrite ; `install` après `stop --unload` ? L'aide de `install --uninstall`
  disait « deux agents » pour trois (**corrigé dans `factory/cli.py`**).
- **2** — `review --list` : « aucun script en attente », mais `calendar plan --replan --dry-run`
  signale un job « auto_approved » à approuver par `factory queue approve`, absent du document.
- **3** — « 2 sources obtenues, 3 exigées » rangé sous « Échoué » alors que `queue status`
  montre 4 jobs *blocked* ; que faire d'un job déjà bloqué ? Tableau du Terminal tronqué.
- **4** — `manual-list` : « Aucune vidéo en attente » ; quel compte pour quelle chaîne ? quelle
  chaîne vend un produit ? que veut dire « Aucun job de rapports » dans `publish status` ?
- **5** — 2 h d'écart dans le document contre « ≥ 240 min entre chaînes de même langue » dans
  `calendar show --check` ; à quoi sert le 3ᵉ jour si le plafond est 2/semaine ?
- **6** — forme de la « décision écrite » ; quoi mettre dans « Niche » et « Compte Google » ;
  « risque de clonage » sur `bms-test`.
- Termes non expliqués : launchd, agent, verrou, fenêtre, jitter, UTC, compartiment, unités,
  OAuth, auto_approved, RIA art. 50, LUFS, empreinte, variante testée, hook, Dossier de départ ;
  statuts en anglais dans le Terminal ; ouverture de `publication.md`.

**Corrections apportées après la passe 1** : table des statuts anglais/français ; conseil plein
écran ; feu vert explicite (« production autorisée ») ; lecture des deux premières lignes de
`daemon status` ; redémarrage par `install` ; cas « auto_approved » avec `queue approve` ; § 8
réécrit en tableau « le message commence par… » (dont les jobs déjà bloqués pour sources) ;
compte et produit d'une chaîne ; message « Aucun job de rapports » ; écart de 4 h entre chaînes
de même langue, rôle du 3ᵉ jour ; tableau des champs « Ajouter une chaîne » ; forme de la
décision écrite ; ouverture de `publication.md` ; 13 entrées de glossaire.

## Rapport de la passe 2 (condensé fidèle)

- Aucune commande en erreur.
- **4** bloqué : « le calendrier donne trois heures (locale, UTC, Paris) et Studio en demande une
  seule » ; « case Contenu altéré ou synthétique : doit-elle être cochée ? »
- **6** bloqué : `config validate` sort 3 « ALERTE rythme de coupe non mesuré » ; la procédure
  disait d'envoyer toute ligne rouge à Thomas. Niche `niche_monetisable_complements_fr` alors
  que la production est en anglais.
- Réserves : message « auto_approved » visible seulement dans `calendar plan` ; « jours
  conseillés » introuvables ; colonnes du Terminal non traduites (Étape, Essais, Pri.,
  Prochaine) ; « pid » et « exporté » hors glossaire ; « thumbnail.py:172 » et « barrière » dans
  le digest (message pour Thomas) ; libellés « Aucune vidéo en attente » et « Aucun script »
  légèrement différents de la sortie réelle.

**Corrections apportées après la passe 2** : fuseau à choisir dans Studio et exemple ; case
synthétique à ne pas modifier ; libellés exacts des messages vides ; colonnes de `queue status`
traduites ; « exporté » et « pid » expliqués ; alertes normales de `config validate` et dernière
ligne attendue ; niche FR exclue ; jours conseillés ; lieu d'apparition du cas « auto_approved ».
**Non corrigé** : le texte technique du digest (« thumbnail.py:172 », « barrière ») — c'est un
message du code, pas du document ; à reformuler côté digest (Thomas).
